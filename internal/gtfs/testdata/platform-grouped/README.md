# Explicit parent-station fixture

This small synthetic feed covers the station display contract. Two platform
stops explicitly reference `central` as `parent_station`, while `north` and an
otherwise similarly named `unparented_central` stop have no parent and remain
standalone. The two platform routes meet at the grouped station, proving that
interchange line membership is retained without name-based grouping.
